<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uplus.order.mapper.FileMapper">

    <select id="getAppendix" resultType="java.lang.String">
        select original_file_name from photo_file where id = #{id}
    </select>
</mapper>
